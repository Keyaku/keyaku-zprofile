# =============================================================================
# debian.lst — Debian/APT-specific /var/lib exclusions
# =============================================================================

# apt package lists — regenerated by `apt update`
var/lib/apt/lists
